@using Microsoft.AspNetCore.Identity
@using Microsoft.Identity.Web
@inject UserManager<IdentityUser> UserManager
@inject SignInManager<IdentityUser> SignInManager

	<ul class="navbar-nav" >

		@if (User.Identity.IsAuthenticated)
    {

		@*<li class="nav-item">
			<span class="nav-link text-dark">Hola, @User.Identity.Name</span>
		</li>
		<li class="nav-item">
			<form class="form-inline" asp-controller="Account" asp-action="SignOut" asp-area="microsoftIdentity">
				<button type="submit" class="nav-link btn btn-link text-dark">
					Logout
				</button>
			</form>
		</li>*@

		<li class="nav-item">
			<span class="navbar-text text-dark">
				<strong>
					Hola @User.Identity.Name.Substring(0,User.Identity.Name.IndexOf("@")) ! 
				</strong>
				</span>
		</li>
		<li class="nav-item">

			@*the azure AD signout action should be a link not a form*@
			<a class="nav-link text-dark" asp-area="MicrosoftIdentity" asp-controller="Account" asp-action="SignOut">Salir</a>
		</li>
        
    }else{

		<li class="nav-item">
			<a class="nav-link text-dark" asp-controller="AppUsuario" asp-action="Cear">Crear</a>
		</li>

		<li class="nav-item">
			<a class="nav-link text-dark" asp-area="microsoftIdentity" asp-controller="Account" asp-action="SignIn">Login</a>
		</li>

		
	}
@*
		@if(SignInManager.IsSignedIn(User))
		{
			<li class="nav-item">
				<span class="nav-link text-dark">Hola, @User.Identity.Name</span>
			</li>

			<li class="nav-item">
				<form class="form-inline" asp-controller="AppUsuarios" asp-action="Logout">
					<button type="submit" class="nav-link btn btn-link text-dark">
						Logout
					</button>
				</form>
		</li>
	}

	else
	{
		<li class="nav-item">
			<a class="nav-link text-dark" asp-controller="usuarios" asp-action="Cear">Crear</a>
		</li>

		<li class="nav-item">
			<a class="nav-link text-dark" asp-controller="usuarios" asp-action="Login">Login</a>
		</li>

	}*@

	</ul>
